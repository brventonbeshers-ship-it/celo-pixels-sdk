name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-celo-pixels-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify celo-pixels-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/celo-pixels-sdk-check

      - name: Initialize project
        working-directory: /tmp/celo-pixels-sdk-check
        run: npm init -y

      - name: Fetch celo-pixels-sdk from registry
        working-directory: /tmp/celo-pixels-sdk-check
        run: npm install celo-pixels-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/celo-pixels-sdk-check
        run: node -e "const sdk = require('celo-pixels-sdk'); console.log('Scheduled health check: celo-pixels-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/celo-pixels-sdk-check
        run: npm ls --all
